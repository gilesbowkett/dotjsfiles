// embarassing to have this in my repo, but it should still be readable

$(document).ready(function() {
  var text = $(".horoscope_listings");
  $("body").html(text);
  $("body").css("margin-left", "20px");
  $("body").css("margin-top", "20px");
});